import { dev } from '$app/environment';

export const title = 'Solync';
export const description = "Solync's landing page and documentation.";
export const url = dev ? 'http://localhost:3000' : 'https://solync.org';
export const ogImage = `${url}/og-image.png`;
